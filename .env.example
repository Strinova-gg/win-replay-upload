# Clerk publishable key for the renderer (Vite exposes VITE_* to the renderer)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# Backend that issues pre-signed GCS URLs
VITE_BACKEND_URL=https://api.example.com
